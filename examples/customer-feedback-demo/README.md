# 客户反馈导航器示例

首次运行命令会创建一个可以随时删除的 Git 仓库，里面只有一份虚构产品说明和五条虚构客户反馈。

这个示例故意没有把产品方向预先确定好。它要展示的是：PDC 不会因为“可以写代码”就把“做一个客户反馈 AI 工具”自动解释成“立刻开始实现”。PDC 应该先恢复已有项目事实，守住唯一正在推进的工作焦点，判断当前应该 Explore、Preview 还是 Engineering，然后给出一个可信的下一步。

创建示例：

```bash
python scripts/pdc_first_run.py demo
```

示例不会包含、读取或要求任何真实客户数据。
